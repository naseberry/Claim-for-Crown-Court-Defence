apiVersion: v1
kind: Service
metadata:
  name: {{ template "cccd.fullname" . }}
  labels:
    app: {{ template "cccd.name" . }}
    chart: {{ template "cccd.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "cccd.name" . }}
    release: {{ .Release.Name }}
